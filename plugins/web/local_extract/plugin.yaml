name: web-local-extract
version: 1.0.0
description: "Local web page extraction using trafilatura + requests. Self-hosted, no API key required. Falls back to Playwright (browser adapter) for Cloudflare-blocked sites."
author: fiik
kind: backend
provides_web_providers:
  - local-extract
